# Web-Crawler
Prints a list of unique email address found from the webpages starting from the start url provided by user. The program stops and the function returns the list when the crawler reaches the limit.

*** Should not be tested in open real web ***
